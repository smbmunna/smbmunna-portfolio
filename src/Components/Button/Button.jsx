

const Button = ({text}) => {
    return (
        <div>
            <button className="btn btn-outline pb-8 pt-1 px-8 rounded-full text-2xl font-bold h-2">{text}</button>
        </div>
    );
};

export default Button;